//! Read-side aggregations. Real impl in phase 2.

pub struct Query;

impl Query {
    pub fn new() -> Self {
        Self
    }
}

impl Default for Query {
    fn default() -> Self {
        Self::new()
    }
}
